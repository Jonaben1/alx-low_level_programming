#include "main.h"

/**
 * _abs - a function that checks for absolute value of a number
 *  @n: the letter being tested
 * Return: Always 0 success
*/

int _abs(int n)
{
	if (n < 0)
		return (-n);
	else
		return (n);
	return (0);
}


